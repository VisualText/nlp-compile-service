{
    "name": "nlp-compile-dispatcher",
    "version": "0.0.1",
    "private": true,
    "description": "Dispatcher worker for the NLP++ cloud compile service.",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "typecheck": "tsc --noEmit"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20250101.0",
        "typescript": "^5.4.0",
        "wrangler": "^3.80.0"
    }
}
